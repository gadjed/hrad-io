{
  "name": "hrad-io",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Top-down IO: harvest, fortify, hold. Sandbox for castle prototypes.",
  "scripts": {
    "start": "node server/index.mjs",
    "dev": "node --watch server/index.mjs",
    "world:gen": "node scripts/generate-settlements.mjs",
    "world:regen": "node scripts/regen-world.mjs",
    "planner:snapshots": "node scripts/export-settlement-snapshots.mjs --load --steps 50 --interval 5 --with-labels --truncate",
    "planner:validate": "node scripts/validate-settlement-planner.mjs data/training/snapshots.jsonl"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2",
    "ws": "^8.18.0"
  }
}
